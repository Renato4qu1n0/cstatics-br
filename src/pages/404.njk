---
layout: layouts/base.njk
permalink: /404.html
title: "Página não encontrada - CSTatics Brasil"
description: "A rota solicitada não existe no CSTatics Brasil."
eleventyExcludeFromCollections: true
---

<section class="notfound">
  <div class="container notfound__inner">
    <div class="notfound__code" aria-hidden="true">
      <span>404</span>
    </div>

    <div class="notfound__text">
      <span class="eyebrow">Rota não encontrada</span>
      <h1 class="notfound__title">Esta página não existe.</h1>
      <p class="notfound__desc">
        O endereço mudou ou o conteúdo ainda não foi publicado. Siga por um dos
        caminhos abaixo.
      </p>

      <div class="notfound__actions">
        <a href="/lineups/" class="btn btn-primary">Ver lineups</a>
        <a href="/tactics/" class="btn btn--secondary">Táticas por mapa</a>
      </div>

      <a href="/" class="notfound__home datum">← Voltar ao início</a>
    </div>
  </div>
</section>

<style>
  .notfound { padding: clamp(3rem, 9vw, 7rem) 0; }

  .notfound__inner {
    display: grid;
    grid-template-columns: auto 1fr;
    gap: clamp(2rem, 6vw, 4.5rem);
    align-items: center;
  }

  .notfound__code {
    font-family: var(--font-display);
    font-size: clamp(5rem, 16vw, 11rem);
    font-weight: 700;
    line-height: 0.85;
    letter-spacing: -0.04em;
    color: transparent;
    -webkit-text-stroke: 2px var(--color-border-light);
    position: relative;
    user-select: none;
  }

  /* eco em âmbar: sinal perdido */
  .notfound__code::after {
    content: '404';
    position: absolute;
    inset: 0;
    color: transparent;
    -webkit-text-stroke: 2px var(--color-primary);
    opacity: 0.35;
    transform: translate(6px, -4px);
  }

  .notfound__title {
    font-size: clamp(1.8rem, 4.5vw, 3rem);
    text-transform: uppercase;
    margin: var(--spacing-md) 0;
  }

  .notfound__desc {
    color: var(--color-text-light);
    font-size: var(--font-size-lg);
    max-width: 46ch;
    line-height: 1.7;
    margin-bottom: var(--spacing-xl);
  }

  .notfound__actions {
    display: flex;
    gap: var(--spacing-md);
    flex-wrap: wrap;
    margin-bottom: var(--spacing-xl);
  }

  .notfound__home {
    font-size: var(--font-size-xs);
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: var(--color-text-muted);
  }

  .notfound__home:hover { color: var(--color-primary); }

  @media (max-width: 720px) {
    .notfound__inner { grid-template-columns: 1fr; }
    .notfound__actions .btn { width: 100%; }
  }
</style>
